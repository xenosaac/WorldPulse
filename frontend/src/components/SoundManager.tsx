'use client';

// TODO: Implement audio feedback
// - Ping sound when new event detected (different pitch per severity)
// - Ambient hum when Live API active
// - Chime when brief complete
// - Mute toggle

export default function SoundManager() {
  // This is a non-visual component. It will use Web Audio API
  // to play sounds in response to events passed via props or context.
  // For now, it renders nothing.
  return null;
}
